<nav class=" navbar navbar-expand-lg navbar-dark" >

  <!-- 管理者と会員でヘッダーリンクの遷移先を変更 -->
  <% if admin_signed_in? %>
    <%= link_to admin_root_path, class: "nav-link text-light" do %>
     ロゴ
    <% end %>
  <% else %>
    <%= link_to root_path, class: "nav-link text-light" do %>
     ロゴ
    <% end %>
  <% end %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav" style="justify-content:flex-end;margin:0 30px">
    <div>
      <div class="row">
        <ul class="navbar-nav ml-auto">

        <!-- 管理者と会員でヘッダーリンクの遷移先を変更 -->
        <!-- 管理者でログインしている場合-->
        <% if admin_signed_in? %>
          <li class="nav-item mx-3">
            <%= link_to '投稿一覧', admin_posts_path, class: "nav-link text-light btn btn-outline-light" %>
          </li>
          <li class="nav-item mx-3">
            <%= link_to '会員一覧', admin_root_path, class: "nav-link text-light btn btn-outline-light" %>
          </li>
          <li class="nav-item mx-3">
            <%= link_to 'ログアウト', destroy_admin_session_path, method: :delete, class: "nav-link text-light btn btn-outline-light" %>
          </li>
        <!-- 会員としてログインしている場合 -->
        <% elsif user_signed_in? %>
          <div class="mt-3 mr-5 text-light" >
            ようこそ、<%= current_user.user_name %>さん!
          </div>
          <li class="nav-item mx-3">
            <%= link_to 'マイページ', users_mypage_path, class: "nav-link text-light btn btn-outline-light" %>
          </li>
          <li class="nav-item mx-3">
            <%= link_to '投稿一覧', posts_path, class: "nav-link text-light btn btn-outline-light" %>
          </li>
          <li class="nav-item mx-3">
            <%= link_to '会員一覧', users_path, class: "nav-link text-light btn btn-outline-light" %>
          </li>
          <li class="nav-item mx-3">
            <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: "nav-link text-light btn btn-outline-light" %>
          </li>
        <!-- 管理者と会員どちらでもログインしていない場合 -->
        <% else %>
          <li class="nav-item mx-3">
            <%= link_to '投稿一覧', posts_path, class: "nav-link text-light btn btn-outline-light" %>
          </li>
          <li class="nav-item mx-3">
            <%= link_to '会員一覧', users_path, class: "nav-link text-light btn btn-outline-light" %>
          </li>
          <li class="nav-item mx-3">
            <%= link_to '新規登録', new_user_registration_path, class: "nav-link text-light btn btn-outline-light" %>
          </li>
          <li class="nav-item mx-3">
            <%= link_to 'ログイン', new_user_session_path, class: "nav-link text-light btn btn-outline-light" %>
          </li>
        <% end %>
        <% if admin_signed_in? %>
          <li>
            <%= render 'admin/tagsearches/tagsearch' %>
          </li>
        <% else %>
          <li>
            <%= render 'public/tagsearches/tagsearch' %>
          </li>
        <% end %>
      </ul>
      </div>
      <div class="row" style="display:flex; justify-content:flex-end; margin-top:5px; margin-right: 3px;">
        <%#= render 'searches/search_form' %>
      </div>
    </div>
  </div>
</nav>
