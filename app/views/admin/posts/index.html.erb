<div class='container'>
  <div class="row">
    <div class="col-md-6">
      <h4 id="heading">投稿一覧</h4>
      投稿数の多いタグ...
      <% @tags.each do |tag| %>
        <%= link_to admin_tagsearches_search_path(tag: tag.name) do %>
          <%= tag.name %>
          <%= "(#{tag.posts.count})" %>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
<div class="container">
  <div class="row">
    <% @posts.each do |post| %>
      <div class="col-md-3 mb-4">
        <div class="card">
          <%= post_image(post, size = "253x200") %>
          <div class="card-body">
            <div class="card-title">
              <strong>Title</strong><br />
              <%= post.title %><br />
              <strong>Tag</strong><br />
              <i class="tag"><%= post.tags.map(&:name).join(',') %></i>
              <div id="favorite_btn_<%= post.id %>" >
                <%= render 'admin/favorites/btn', post: post %>
              </div>
              <div><%= render 'admin/comments/counter', post: post %></div>
            </div>
            <%= link_to "詳細", admin_post_path(post.id), class: "btn btn-success" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
  <div class="row">
    <div class="col-md-1 mx-auto">
      <div class="pagination">
        <%= paginate @posts %>
      </div>
    </div>
  </div>
</div>



