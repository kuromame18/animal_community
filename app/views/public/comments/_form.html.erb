<%= form_with model: [post, comment], local: false do |f| %>
  <%= f.text_area :comment, placeholder: "コメントをここに", size: '50x5', id: "comment_textarea" %>
  <%= f.submit "送信する", class: "btn btn-success" %>
<% end %>
